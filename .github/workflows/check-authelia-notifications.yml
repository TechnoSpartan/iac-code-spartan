name: Check Authelia Notifications

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check Authelia notifications
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "════════════════════════════════════════════════════════════"
            echo "📧 CHECKING NOTIFICATIONS FILE"
            echo "════════════════════════════════════════════════════════════"

            if [ -f /opt/codespartan/platform/authelia/data/notifications.txt ]; then
              echo "✅ Notifications file exists"
              echo ""
              echo "Last 20 notifications:"
              tail -20 /opt/codespartan/platform/authelia/data/notifications.txt
            else
              echo "❌ No notifications file found"
              echo ""
              echo "Checking if data directory exists:"
              ls -la /opt/codespartan/platform/authelia/data/ || echo "Data directory does not exist"
            fi

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "📋 AUTHELIA LOGS (last 30 lines)"
            echo "════════════════════════════════════════════════════════════"
            docker logs authelia --tail 30
