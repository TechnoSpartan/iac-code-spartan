name: Generate New Authelia Password Hash

on:
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate and update password
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail

            echo "════════════════════════════════════════════════════════════"
            echo "🔐 GENERATING NEW PASSWORD HASH"
            echo "════════════════════════════════════════════════════════════"

            # Generate new hash for 'codespartan123'
            NEW_HASH=$(docker exec authelia authelia crypto hash generate argon2 --password 'codespartan123' 2>&1 | grep 'Digest:' | awk '{print $2}')

            echo "New hash generated: $NEW_HASH"

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "📝 CURRENT users_database.yml"
            echo "════════════════════════════════════════════════════════════"
            cat /opt/codespartan/platform/authelia/users_database.yml

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "✏️  UPDATING users_database.yml"
            echo "════════════════════════════════════════════════════════════"

            # Create backup
            cp /opt/codespartan/platform/authelia/users_database.yml /opt/codespartan/platform/authelia/users_database.yml.backup

            # Update the password field with new hash
            sed -i "s|password: \".*\"|password: \"$NEW_HASH\"|" /opt/codespartan/platform/authelia/users_database.yml

            echo "Updated users_database.yml:"
            cat /opt/codespartan/platform/authelia/users_database.yml

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "🔄 RECREATING AUTHELIA CONTAINER"
            echo "════════════════════════════════════════════════════════════"

            cd /opt/codespartan/platform/authelia
            docker compose down
            docker compose up -d

            echo "⏳ Waiting 15 seconds for Authelia to start..."
            sleep 15

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "✅ VERIFYING NEW HASH"
            echo "════════════════════════════════════════════════════════════"

            # Extract the new hash from the file
            CURRENT_HASH=$(grep -A 1 "password:" /opt/codespartan/platform/authelia/users_database.yml | tail -1 | sed 's/.*password: "\([^"]*\)".*/\1/')

            echo "Hash in database: $CURRENT_HASH"
            echo ""
            echo "Testing password validation..."

            docker exec authelia authelia crypto hash validate argon2 \
              --password 'codespartan123' \
              -- "$CURRENT_HASH" && echo "✅ PASSWORD IS VALID!" || echo "❌ PASSWORD VALIDATION FAILED"

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "📊 CONTAINER STATUS"
            echo "════════════════════════════════════════════════════════════"
            docker ps --filter "name=authelia" --format "table {{.Names}}\t{{.Status}}"
