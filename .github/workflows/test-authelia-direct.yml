name: Test Authelia Direct Access (Bypass Traefik)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test direct access to Authelia
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "════════════════════════════════════════════════════════════"
            echo "🧪 TESTING AUTHELIA DIRECT ACCESS"
            echo "════════════════════════════════════════════════════════════"

            # Get Authelia IP
            AUTHELIA_IP=$(docker inspect authelia --format='{{range $net,$v := .NetworkSettings.Networks}}{{if eq $net "web"}}{{$v.IPAddress}}{{end}}{{end}}')
            echo "Authelia IP in web network: $AUTHELIA_IP"
            echo ""

            echo "────────────────────────────────────────────────────────────"
            echo "Test 1: Health API"
            echo "────────────────────────────────────────────────────────────"
            curl -s -m 5 http://${AUTHELIA_IP}:9091/api/health
            echo ""
            echo ""

            echo "────────────────────────────────────────────────────────────"
            echo "Test 2: Root endpoint (should return HTML)"
            echo "────────────────────────────────────────────────────────────"
            curl -s -m 5 -o /dev/null -w "HTTP Status: %{http_code}, Time: %{time_total}s\n" http://${AUTHELIA_IP}:9091/
            echo ""

            echo "────────────────────────────────────────────────────────────"
            echo "Test 3: Root endpoint with Host header"
            echo "────────────────────────────────────────────────────────────"
            curl -s -m 5 -o /dev/null -w "HTTP Status: %{http_code}, Time: %{time_total}s\n" \
              -H "Host: auth.mambo-cloud.com" \
              http://${AUTHELIA_IP}:9091/
            echo ""

            echo "────────────────────────────────────────────────────────────"
            echo "Test 4: From Traefik container"
            echo "────────────────────────────────────────────────────────────"
            docker exec traefik wget -O- --timeout=5 http://authelia:9091/ 2>&1 | head -10
            echo ""

            echo "────────────────────────────────────────────────────────────"
            echo "Test 5: Check Traefik router for authelia"
            echo "────────────────────────────────────────────────────────────"
            docker exec traefik wget -qO- http://localhost:8080/api/http/routers 2>/dev/null | grep -i authelia || echo "No authelia router found"
