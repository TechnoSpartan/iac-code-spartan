name: Update Authelia Configuration File

on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy configuration file to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "codespartan/platform/authelia/configuration.yml"
          target: "/tmp/authelia-new-config.yml"
          strip_components: 3
          overwrite: true

      - name: Update Authelia configuration
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "════════════════════════════════════════════════════════════"
            echo "🔧 UPDATING AUTHELIA CONFIGURATION"
            echo "════════════════════════════════════════════════════════════"

            # Create backup
            sudo cp /opt/codespartan/platform/authelia/configuration.yml /opt/codespartan/platform/authelia/configuration.yml.backup

            # Replace configuration with new OAuth2/OIDC enabled version
            sudo mv /tmp/authelia-new-config.yml/configuration.yml /opt/codespartan/platform/authelia/configuration.yml
            sudo chown root:root /opt/codespartan/platform/authelia/configuration.yml

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "🔄 RESTARTING AUTHELIA"
            echo "════════════════════════════════════════════════════════════"

            cd /opt/codespartan/platform/authelia
            docker compose restart authelia

            echo "⏳ Waiting 10 seconds for Authelia to restart..."
            sleep 10

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "✅ VERIFICATION"
            echo "════════════════════════════════════════════════════════════"

            docker ps --filter "name=authelia" --format "table {{.Names}}\t{{.Status}}"

            echo ""
            echo "📋 Last 10 log lines:"
            docker logs authelia --tail 10
