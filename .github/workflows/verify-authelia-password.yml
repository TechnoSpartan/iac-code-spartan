name: Verify Authelia Password Hash

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Verify password hash
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "════════════════════════════════════════════════════════════"
            echo "👤 USERS DATABASE"
            echo "════════════════════════════════════════════════════════════"
            cat /opt/codespartan/platform/authelia/users_database.yml

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "🔐 VERIFY PASSWORD HASH"
            echo "════════════════════════════════════════════════════════════"

            # Extract hash from users_database.yml
            HASH=$(grep -A 1 "password:" /opt/codespartan/platform/authelia/users_database.yml | tail -1 | sed 's/.*"\(.*\)".*/\1/')

            echo "Testing password: codespartan123"
            docker exec authelia authelia crypto hash validate argon2 \
              --password 'codespartan123' \
              --hash "$HASH" || echo "Password validation failed"

            echo ""
            echo "════════════════════════════════════════════════════════════"
            echo "🆕 GENERATE NEW HASH"
            echo "════════════════════════════════════════════════════════════"
            echo "Generating hash for: codespartan123"
            docker exec authelia authelia crypto hash generate argon2 \
              --password 'codespartan123'
