# OpenProject Docker Configuration
# ⚠️  IMPORTANT: This file is for REFERENCE ONLY
#
# Create a .env file locally (DO NOT COMMIT to git):
#   cp .env.example .env
#   # Edit .env with REAL production values
#   # Keep .env in .gitignore (never commit credentials)
#
# For GitHub deployment, add secrets in GitHub Settings:
#   Settings → Secrets and variables → Actions
#
# Add these secrets:
#   - OPENPROJECT_POSTGRES_PASSWORD
#   - OPENPROJECT_SECRET_KEY_BASE
#   - OPENPROJECT_SMTP_PASSWORD

# ============================================
# Traefik Configuration
# ============================================
# The hostname for this OpenProject instance
TRAEFIK_HOSTNAME=project.codespartan.cloud

# ============================================
# PostgreSQL Configuration
# ============================================
# Database name
POSTGRES_DB=openproject

# Database user
POSTGRES_USER=openproject

# Database password - CHANGE THIS TO A STRONG PASSWORD!
# Generate with: openssl rand -hex 16
POSTGRES_PASSWORD=change_me_to_a_strong_password_12345

# ============================================
# OpenProject Configuration
# ============================================
# External hostname (same as TRAEFIK_HOSTNAME for production)
OPENPROJECT_HOST__NAME=project.codespartan.cloud

# Default language (en, es, de, fr, etc.)
OPENPROJECT_DEFAULT__LANGUAGE=en

# Rails Secret Key Base - CHANGE THIS!
# Generate with: openssl rand -hex 64
OPENPROJECT_SECRET__KEY__BASE=change_me_to_a_long_random_string_min_64_characters_generated_with_openssl

# ============================================
# Email Configuration (SMTP)
# ============================================
# Email delivery method: 'smtp'
OPENPROJECT_EMAIL__DELIVERY__METHOD=smtp

# SMTP server address (Hostinger)
OPENPROJECT_SMTP__ADDRESS=smtp.hostinger.com

# SMTP port (465 for SSL/TLS)
OPENPROJECT_SMTP__PORT=465

# SMTP domain
OPENPROJECT_SMTP__DOMAIN=codespartan.es

# SMTP authentication method: 'login'
OPENPROJECT_SMTP__AUTHENTICATION=login

# SMTP username (your Hostinger email address)
OPENPROJECT_SMTP__USER__NAME=noreply@codespartan.es

# SMTP password - GET FROM YOUR HOSTINGER CONTROL PANEL
# ⚠️  DO NOT commit this to git - use .gitignore and GitHub Secrets
OPENPROJECT_SMTP__PASSWORD=change_me_to_hostinger_email_password

# Enable STARTTLS encryption (use implicit TLS with port 465)
OPENPROJECT_SMTP__ENABLE__STARTTLS=false

# ============================================
# Database URL (derived from above - don't change)
# ============================================
# Format: postgres://user:password@host:port/database
# Note: This uses the password from POSTGRES_PASSWORD above
DATABASE_URL=postgres://openproject:change_me_to_a_strong_password_12345@db:5432/openproject
