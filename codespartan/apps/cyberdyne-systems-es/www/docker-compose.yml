# RETIRED on main VPS (2026-07-26).
# Frontend + API now run on CodeSpartan-apis (10.0.0.3:3080 / :3081)
# and are routed by Traefik file provider (platform/traefik/dynamic/dynamic-config.yml).
#
# Keep this file only as a stub so old deploy workflows fail safely if re-run.
services: {}
